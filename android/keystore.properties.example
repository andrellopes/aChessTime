## Exemplo de configuração do keystore (NÃO COMMITAR A VERSÃO REAL)
# Caminho para o arquivo JKS/keystore (relativo ao root do projeto ou absoluto)
storeFile=android/app/chess_time_release.jks

# Senha do keystore
storePassword=YOUR_STORE_PASSWORD

# Alias da chave
keyAlias=chess_time

# Senha da chave
keyPassword=YOUR_KEY_PASSWORD

# Opcional: ID do app do AdMob para Android
# Use o ID real apenas no ambiente local/CI via variáveis de ambiente/arquivo não versionado
ADMOB_APP_ID_ANDROID=ca-app-pub-3940256099942544~3347511713

# Como usar:
# 1) Copie este arquivo para 'android/keystore.properties' (não versione!)
# 2) Preencha com seus valores reais
# 3) Garanta que *.jks e keystore.properties estejam ignorados no .gitignore